# PostgreSQL em produção; SQLite é permitido apenas para demonstração local.
DATABASE_URL=sqlite:///./cloudops.db
# Use o segredo de assinatura do webhook criado na Shopify; nunca versione o valor real.
SHOPIFY_WEBHOOK_SECRET=
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
